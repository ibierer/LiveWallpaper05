<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.software.live_wallpaper" />
    <uses-feature android:glEsVersion="0x00032000" android:required="true" />

    <application
        android:name="com.vizbox4d.activewallpaperdata.ActiveWallpaperApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_toroids"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_toroids"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveWallpaper05"
        tools:targetApi="34">
        <activity
            android:name="com.vizbox4d.PreviewActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
