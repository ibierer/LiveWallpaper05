<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <solid android:color="#77FFFFFF" /> <!-- Background color -->
        </shape>
    </item>

    <!--<item android:id="@android:id/secondaryProgress">
        <clip>
            <shape android:shape="rectangle">
                <size android:height="2dp" /> &lt;!&ndash; Adjust the height here &ndash;&gt;
                <solid android:color="@color/seek_bar_color" /> &lt;!&ndash; Secondary progress color &ndash;&gt;
            </shape>
        </clip>
    </item>-->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/seek_bar_color" /> <!-- Progress color -->
            </shape>
        </clip>
    </item>

</layer-list>
